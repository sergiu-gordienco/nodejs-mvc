doctype html
html(lang="en")
  head
    title= pageTitle
    script(type='text/javascript').
      if (foo) {
         bar(1 + 5)
      }
    style(type="text/css").
        h1 u { color: green; }
  body
    h1 Jade - node template engine 
        u you got #{vars.pageviews} pageviews
    #container.col
      if youAreUsingJade
        p You are amazing
      else
        p Get on it!
      p.
        Jade is a terse and simple
        templating language with a
        strong focus on performance
        and powerful features.
    h2 Posted Code
    pre
        code #{JSON.stringify({env : env, vars: vars}, null, "    ")}
